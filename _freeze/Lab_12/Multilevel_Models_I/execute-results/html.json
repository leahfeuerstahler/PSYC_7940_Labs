{
  "hash": "09176be6d0abbde4a7ec80eae72b32ad",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"Lab 12: Multilevel Models I\"\ndate: April 22, 2026\n---\n\n\n\n\n## Overview\n\n-   Today we will learn how to fit multilevel models in `brms`.\n\n-   In previous labs, each parameter applied to everyone included in the model. However, this method ignores within-person variation. \n\n-   To allow for individual differences, we can use multilevel modeling.\n\n-   Instead of using a separate intercept for each observation, the multilevel approach uses the method of partial pooling to model a distribution of intercepts. \n\n## Multilevel Models\n\nTo illustrate, let's load in the `TDF.csv` data, along with the `tidyverse` and `brms` packages. \n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(tidyverse); library(brms)\nd1 <- read_csv(\"TDF.csv\")\n```\n:::\n\n\n::: columns\n:::: column\n\nPreviously:\n\n$$\\omega_i \\sim Normal (\\mu_i, \\sigma)$$\n\n$$\\mu_i = a + b \\times hc_i$$\n\n$a \\sim N(66.5, 2)$\n\n$b \\sim N(1, .5)$\n\n$\\sigma \\sim Exp(.25)$\n\n::::\n:::: column\n\nNow: \n\n$$\\omega_i \\sim Normal (\\mu_i, \\sigma)$$\n\n$$\\mu_i = a + b \\times hc_i$$\n$a \\sim N(\\bar{a}, \\sigma_a)$\n\n$\\bar{a} \\sim N(66.5, 2)$\n\n$\\sigma_a \\sim ???$\n\n$b \\sim N(1, .5)$\n\n$\\sigma \\sim Exp(.25)$\n\n::::\n:::\n\n\n\n## Multilevel Models\n\nWe can allow for random intercepts using the `(1 | ID)` syntax, where `ID` is the clustering variable. \n\n* This is the same syntax used by `lmer` and `glmer` for multilevel models.\n\nFor our height and weight example, our formula looks like this: \n\n\n::: {.cell}\n\n```{.r .cell-code}\nbrm_form1 <- Weight ~ HeightC + (1 | Name) \n```\n:::\n\n\n## Priors\n\nUse `get_prior` to see how parameters are represented.\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nget_prior(brm_form1, d1, \n          family = gaussian(link = \"identity\"))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n                 prior     class      coef group resp dpar nlpar lb ub tag\n                (flat)         b                                          \n                (flat)         b   HeightC                                \n student_t(3, 70, 7.4) Intercept                                          \n  student_t(3, 0, 7.4)        sd                                  0       \n  student_t(3, 0, 7.4)        sd            Name                  0       \n  student_t(3, 0, 7.4)        sd Intercept  Name                  0       \n  student_t(3, 0, 7.4)     sigma                                  0       \n       source\n      default\n (vectorized)\n      default\n      default\n (vectorized)\n (vectorized)\n      default\n```\n\n\n:::\n:::\n\n\n\n## Priors\n\nAs we see in the `class` column, we have a new parameter class labelled `sd`. This is the prior for the standard deviation of intercepts.\n\n* Recall: intercept = predicted weight for average height\n\n* We may expect some individual variability. We will try an `Exponential(.5)`. \n\n\n::: {.cell}\n\n```{.r .cell-code}\nprior1 <- prior(normal(66.5, 2), class = \"Intercept\") +\n          prior(exponential(.5), class = \"sd\") +\n          prior(normal(1, .5), class = \"b\") +\n          prior(exponential(.25), class = \"sigma\")\n```\n:::\n\n\n## Prior Predictive Simulation\n\n\n::: {.cell}\n\n```{.r .cell-code}\npredict1 <- brm(brm_form1, d1,\n                family = gaussian(link = \"identity\"),\n                prior = prior1, cores = 4, sample_prior = \"only\")\n```\n:::\n\n\n## Prior Predictive Simulation\n\nOur choice of `sd` prior affects the width of the band of predicted observations, so we want to make sure this makes sense.\n\n* `method = \"posterior_predict\"`: plot the distribution of predicted observations\n\n* `re_formula = NULL`: include all cluster effects\n\n\n::: {.cell}\n\n```{.r .cell-code}\nconditional_effects(predict1, method = \"posterior_predict\", re_formula = NULL)\n```\n\n::: {.cell-output-display}\n![](Multilevel_Models_I_files/figure-revealjs/unnamed-chunk-7-1.png){width=960}\n:::\n:::\n\n\n\n\n## Model Fitting\n\n\n::: {.cell}\n\n```{.r .cell-code}\nm1 <- brm(brm_form1, d1,\n          family = gaussian(link = \"identity\"),\n          prior = prior1, cores = 4)\n```\n:::\n\n\n## Model Results\n\nNotice that even for this relatively simple multilevel model, estimation is not as efficient as with other models. We need to be cautious to run enough iterations for the inferences we want.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsummary(m1)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n Family: gaussian \n  Links: mu = identity \nFormula: Weight ~ HeightC + (1 | Name) \n   Data: d1 (Number of observations: 179) \n  Draws: 4 chains, each with iter = 2000; warmup = 1000; thin = 1;\n         total post-warmup draws = 4000\n\nMultilevel Hyperparameters:\n~Name (Number of levels: 179) \n              Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nsd(Intercept)     1.32      0.99     0.03     3.61 1.05       71       32\n\nRegression Coefficients:\n          Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nIntercept    69.29      0.36    68.58    70.00 1.00     1823     2296\nHeightC       0.78      0.05     0.68     0.89 1.00     2153     1578\n\nFurther Distributional Parameters:\n      Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nsigma     4.56      0.46     3.36     5.27 1.05       75       26\n\nDraws were sampled using sampling(NUTS). For each parameter, Bulk_ESS\nand Tail_ESS are effective sample size measures, and Rhat is the potential\nscale reduction factor on split chains (at convergence, Rhat = 1).\n```\n\n\n:::\n:::\n\n\n\n## Model Results\n\n* $\\bar{a}$ is the overall mean intercept.\n\n* The **random effects** (`ranef`) are individuals' deviations from the mean intercept.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nindividual_intercepts <- ranef(m1)$Name[,\"Estimate\",]\nggplot() + geom_density(aes(x = individual_intercepts))\n```\n\n::: {.cell-output-display}\n![](Multilevel_Models_I_files/figure-revealjs/unnamed-chunk-10-1.png){width=960}\n:::\n:::\n\n\n## Another Example\n\n\nLet's now look at the more complicated `langauge.csv` example from lecture. \n\n\n::: {.cell}\n\n```{.r .cell-code}\nd2 <- read_csv(\"language.csv\")\n```\n:::\n\n\nWe want to estimate index variables for both task type and learning group type, and we want individuals to have random intercepts.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbrm_form2 <- bf(sumscore|trials(nitems) ~ a + b + c,\n                a ~ 0 + factor(tasktype),\n                b ~ 0 + factor(group),\n                c ~ (1|ID), nl = TRUE)\n```\n:::\n\n\n## Priors\n\nRemember that we fit a multilevel binomial model for these data.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nget_prior(brm_form2, d2,\n          family = binomial (link = \"logit\"))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n                prior class                 coef group resp dpar nlpar lb ub\n               (flat)     b                                          a      \n               (flat)     b  factortasktypePSTps                     a      \n               (flat)     b  factortasktypesGJTG                     a      \n               (flat)     b  factortasktypesGJTU                     a      \n               (flat)     b  factortasktypewGJTG                     a      \n               (flat)     b  factortasktypewGJTU                     a      \n               (flat)     b                                          b      \n               (flat)     b factorgroupclassroom                     b      \n               (flat)     b factorgroupimmersion                     b      \n               (flat)     b    factorgroupnative                     b      \n               (flat)     b                                          c      \n               (flat)     b            Intercept                     c      \n student_t(3, 0, 2.5)    sd                                          c  0   \n student_t(3, 0, 2.5)    sd                         ID               c  0   \n student_t(3, 0, 2.5)    sd            Intercept    ID               c  0   \n tag       source\n          default\n     (vectorized)\n     (vectorized)\n     (vectorized)\n     (vectorized)\n     (vectorized)\n          default\n     (vectorized)\n     (vectorized)\n     (vectorized)\n          default\n     (vectorized)\n          default\n     (vectorized)\n     (vectorized)\n```\n\n\n:::\n:::\n\n\n\n## Priors\n\nIt is a long list, but we can simplify matters by specifying the same priors for each set of index variables. There is no need to specify priors for each `coef` (unless you want to!). Here, we use the priors from lecture.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nprior2 <- prior(normal(3, .5), class = \"b\", coef = \"Intercept\", nlpar = \"c\") +\n          prior(normal(0, .5), class = \"b\", nlpar = \"a\") +\n          prior(normal(0, .5), class = \"b\", nlpar = \"b\") +\n          prior(exponential(2), class = \"sd\", nlpar = \"c\")\n```\n:::\n\n\n## Model Fitting & Results\n\n\n::: {.cell messsage='true'}\n\n```{.r .cell-code}\nm2 <- brm(brm_form2, d2,\n          family = binomial (link = \"logit\"),\n          prior = prior2)\n```\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsummary(m2)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n``` nowrap-code\n Family: binomial \n  Links: mu = logit \nFormula: sumscore | trials(nitems) ~ a + b + c \n         a ~ 0 + factor(tasktype)\n         b ~ 0 + factor(group)\n         c ~ (1 | ID)\n   Data: d2 (Number of observations: 675) \n  Draws: 4 chains, each with iter = 2000; warmup = 1000; thin = 1;\n         total post-warmup draws = 4000\n\nMultilevel Hyperparameters:\n~ID (Number of levels: 135) \n                Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nsd(c_Intercept)     0.57      0.04     0.49     0.66 1.00     1085     1947\n\nRegression Coefficients:\n                       Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS\na_factortasktypePSTps      0.83      0.21     0.42     1.23 1.01      943\na_factortasktypesGJTG     -0.36      0.21    -0.76     0.05 1.01      946\na_factortasktypesGJTU     -0.93      0.21    -1.34    -0.53 1.01      937\na_factortasktypewGJTG      0.83      0.22     0.41     1.25 1.01      940\na_factortasktypewGJTU     -0.65      0.21    -1.05    -0.24 1.01      923\nb_factorgroupclassroom    -0.48      0.26    -1.00     0.04 1.00     1506\nb_factorgroupimmersion    -0.38      0.26    -0.91     0.12 1.00     1474\nb_factorgroupnative        0.55      0.27     0.01     1.08 1.01     1412\nc_Intercept                2.74      0.29     2.18     3.31 1.01     1381\n                       Tail_ESS\na_factortasktypePSTps      1602\na_factortasktypesGJTG      1720\na_factortasktypesGJTU      1647\na_factortasktypewGJTG      1741\na_factortasktypewGJTU      1305\nb_factorgroupclassroom     2196\nb_factorgroupimmersion     2251\nb_factorgroupnative        2134\nc_Intercept                1757\n\nDraws were sampled using sampling(NUTS). For each parameter, Bulk_ESS\nand Tail_ESS are effective sample size measures, and Rhat is the potential\nscale reduction factor on split chains (at convergence, Rhat = 1).\n```\n\n\n:::\n:::\n\n\n\n## Model Results\n\nExtracting \"fixed effects\" give the index variable values and the mean intercept.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsummary(m2)$fixed\n```\n\n::: {.cell-output .cell-output-stdout}\n\n``` nowrap-code\n                         Estimate Est.Error     l-95% CI    u-95% CI     Rhat\na_factortasktypePSTps   0.8273592 0.2133156  0.416038485  1.23317679 1.005767\na_factortasktypesGJTG  -0.3569223 0.2122366 -0.759365967  0.05443398 1.005682\na_factortasktypesGJTU  -0.9337774 0.2121041 -1.335773294 -0.52662121 1.005944\na_factortasktypewGJTG   0.8336337 0.2161935  0.412506390  1.25016447 1.005562\na_factortasktypewGJTU  -0.6472950 0.2124010 -1.054419648 -0.23874083 1.005756\nb_factorgroupclassroom -0.4775356 0.2641204 -1.001250184  0.03564102 1.001764\nb_factorgroupimmersion -0.3811829 0.2649734 -0.913519447  0.12146036 1.002147\nb_factorgroupnative     0.5509881 0.2687100  0.007598725  1.08424068 1.005012\nc_Intercept             2.7373258 0.2924177  2.177130887  3.31002980 1.005448\n                        Bulk_ESS Tail_ESS\na_factortasktypePSTps   943.1134 1602.491\na_factortasktypesGJTG   945.5393 1719.553\na_factortasktypesGJTU   936.5631 1646.777\na_factortasktypewGJTG   940.3324 1740.788\na_factortasktypewGJTU   923.3061 1305.032\nb_factorgroupclassroom 1506.1447 2196.315\nb_factorgroupimmersion 1474.4888 2250.586\nb_factorgroupnative    1411.6226 2134.182\nc_Intercept            1381.3190 1756.626\n```\n\n\n:::\n:::\n\n\n## Model Results\n\n\"Random effects\" gives the standard deviation of the random intercepts.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsummary(m2)$random\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n$ID\n                 Estimate  Est.Error  l-95% CI  u-95% CI     Rhat Bulk_ESS\nsd(c_Intercept) 0.5687298 0.04327678 0.4896202 0.6586102 1.000712 1084.905\n                Tail_ESS\nsd(c_Intercept) 1947.465\n```\n\n\n:::\n:::\n\n\n## Adding On\n\nWe can set multiple types of random effects. For example, we could consider the random effects of both persons and items.\n\nFor example:\n\n\n::: {.cell}\n\n```{.r .cell-code}\nd3 <- read.csv(\"language item.csv\")\nbrm_form3 <- bf(response ~ a + b + c + d,\n                    a ~ 0 + tasktype,\n                    b ~ 0 + group,\n                    c ~ (1 | ID),\n                    d ~ 0 + (1 | item), nl = TRUE)\n```\n:::\n\n\n## Priors\n\nNow, we can set separate `sd` parameters for two different `group` values: `ID` and `item`:\n\n\n::: {.cell}\n\n```{.r .cell-code}\nget_prior(brm_form3, d3, \n          family = bernoulli(link = \"logit\"))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n                prior class           coef group resp dpar nlpar lb ub tag\n               (flat)     b                                    a          \n               (flat)     b    tasktypePST                     a          \n               (flat)     b   tasktypesGJT                     a          \n               (flat)     b   tasktypewGJT                     a          \n               (flat)     b                                    b          \n               (flat)     b groupclassroom                     b          \n               (flat)     b groupimmersion                     b          \n               (flat)     b    groupnative                     b          \n               (flat)     b                                    c          \n               (flat)     b      Intercept                     c          \n student_t(3, 0, 2.5)    sd                                    c  0       \n student_t(3, 0, 2.5)    sd                   ID               c  0       \n student_t(3, 0, 2.5)    sd      Intercept    ID               c  0       \n student_t(3, 0, 2.5)    sd                                    d  0       \n student_t(3, 0, 2.5)    sd                 item               d  0       \n student_t(3, 0, 2.5)    sd      Intercept  item               d  0       \n       source\n      default\n (vectorized)\n (vectorized)\n (vectorized)\n      default\n (vectorized)\n (vectorized)\n (vectorized)\n      default\n (vectorized)\n      default\n (vectorized)\n (vectorized)\n      default\n (vectorized)\n (vectorized)\n```\n\n\n:::\n:::\n\n\n## Priors\n\nAgain, we'll use the same priors that we did in lecture.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nprior3 <- prior(normal(0, .5), class = \"b\", nlpar = \"a\") +\n  prior(normal(0, .5), class = \"b\", nlpar = \"b\") +\n  prior(exponential(1), class = \"sd\", nlpar = \"c\") +\n  prior(normal(3, .5), class = \"b\", coef = \"Intercept\", nlpar = \"c\") +\n  prior(exponential(2), class = \"sd\", nlpar = \"d\")\n```\n:::\n\n\nThe following model takes **much** longer to run: **BE WARNED**!!!\n\n\n::: {.cell}\n\n```{.r .cell-code}\nm3 <- brm(brm_form3, d3,\n          family = bernoulli(link = \"logit\"),\n          prior = prior3, cores = 4)\n```\n:::\n\n\n## Model Results\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsummary(m3)$fixed\n```\n\n::: {.cell-output .cell-output-stdout}\n\n``` nowrap-code\n                   Estimate Est.Error   l-95% CI   u-95% CI     Rhat  Bulk_ESS\na_tasktypePST     0.8951641 0.2855012  0.3514294  1.4574956 1.001545  918.2094\na_tasktypesGJT   -0.7608174 0.2713616 -1.2734321 -0.2325683 1.001637 1068.6132\na_tasktypewGJT   -0.1655757 0.2710358 -0.6819681  0.3690380 1.002021 1062.3832\nb_groupclassroom -0.4006487 0.2721992 -0.9437418  0.1461100 1.002057  972.9028\nb_groupimmersion -0.2932988 0.2774942 -0.8314684  0.2636392 1.003976  993.6550\nb_groupnative     0.6551979 0.2721356  0.1152191  1.1940233 1.002936 1102.0320\nc_Intercept       2.9650694 0.3336365  2.3247430  3.6222295 1.002058 1085.2117\n                 Tail_ESS\na_tasktypePST    1672.248\na_tasktypesGJT   1958.611\na_tasktypewGJT   2020.982\nb_groupclassroom 1676.706\nb_groupimmersion 1766.733\nb_groupnative    1763.795\nc_Intercept      1987.187\n```\n\n\n:::\n:::\n\n\n\n## Model Results\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsummary(m3)$random\n```\n\n::: {.cell-output .cell-output-stdout}\n\n``` nowrap-code\n$ID\n                 Estimate  Est.Error  l-95% CI  u-95% CI     Rhat Bulk_ESS\nsd(c_Intercept) 0.5916929 0.04530542 0.5085156 0.6825005 1.003104 761.9798\n                Tail_ESS\nsd(c_Intercept) 1715.152\n\n$item\n                 Estimate  Est.Error  l-95% CI u-95% CI     Rhat Bulk_ESS\nsd(d_Intercept) 0.9076495 0.08069758 0.7643069 1.068791 1.004582   568.17\n                Tail_ESS\nsd(d_Intercept) 1335.764\n```\n\n\n:::\n:::\n\n\n## Model Results\n\nAs we can see, the variation of items is much larger than the variation of individuals!\n\n\n::: {.cell}\n\n```{.r .cell-code}\nmean_ind3 <- ranef(m3)$ID[, \"Estimate\", ]\nmean_item3 <- ranef(m3)$item[, \"Estimate\", ]\n\nggplot()+\n  geom_density(aes(x = mean_ind3), color = \"red\")+\n  geom_density(aes(x = mean_item3), color = \"blue\")\n```\n\n::: {.cell-output-display}\n![](Multilevel_Models_I_files/figure-revealjs/unnamed-chunk-24-1.png){width=960}\n:::\n:::\n\n\n## Activity\n\n1. Modify `m3` so that items are treated as a fixed effect rather than a random effect. Compare the output to that of `m3`. \n\n2. Modify `m2` so that `tasktype` is a random effect rather than a fixed effect. How does this affect model run time and convergence? How does this affect the interpretations that you make?",
    "supporting": [],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {
      "include-after-body": [
        "\n<script>\n  // htmlwidgets need to know to resize themselves when slides are shown/hidden.\n  // Fire the \"slideenter\" event (handled by htmlwidgets.js) when the current\n  // slide changes (different for each slide format).\n  (function () {\n    // dispatch for htmlwidgets\n    function fireSlideEnter() {\n      const event = window.document.createEvent(\"Event\");\n      event.initEvent(\"slideenter\", true, true);\n      window.document.dispatchEvent(event);\n    }\n\n    function fireSlideChanged(previousSlide, currentSlide) {\n      fireSlideEnter();\n\n      // dispatch for shiny\n      if (window.jQuery) {\n        if (previousSlide) {\n          window.jQuery(previousSlide).trigger(\"hidden\");\n        }\n        if (currentSlide) {\n          window.jQuery(currentSlide).trigger(\"shown\");\n        }\n      }\n    }\n\n    // hookup for slidy\n    if (window.w3c_slidy) {\n      window.w3c_slidy.add_observer(function (slide_num) {\n        // slide_num starts at position 1\n        fireSlideChanged(null, w3c_slidy.slides[slide_num - 1]);\n      });\n    }\n\n  })();\n</script>\n\n"
      ]
    },
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}